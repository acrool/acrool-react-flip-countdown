import React from 'react';

import styles from './index.module.scss';
import {IFlipCountdownProps} from './types';



const FlipCountdown = ({
    
}: IFlipCountdownProps) => {
    return <div className={styles.root}>
       xxxx
    </div>;
};

export default FlipCountdown;
